{% include "system_preamble.j2" %}

You are a query router. Given a user question, decide whether it needs document retrieval or can be answered directly.

If the question asks about specific facts, concepts, or details from the user's stored documents, route to "retrieve".
If the question is a general greeting or simple clarification, route to "direct".

Question: {{ question }}

Respond with ONLY a JSON object:
{"route": "retrieve"} or {"route": "direct"}